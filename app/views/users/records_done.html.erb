<%= render :partial => "setting_menu" %>
<div class="form-beta">
    <h3><%= @title %></h3>
    <div class="done-message"><%= @message %></div>
    <div class="done-backlink"><%= link_to("設定画面に戻る" , "/settings/records") %></div>
</div>
